% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group.R
\name{split.data.table}
\alias{split.data.table}
\title{Split a data table, with a log}
\usage{
\method{split}{data.table}(x, f, drop = FALSE, ...)
}
\arguments{
\item{x}{The data table to split.}

\item{f}{The grouping factor, as in \code{\link[base:split]{base::split()}}. \code{data.table}'s own
\verb{by=} is the usual way to give it instead.}

\item{drop}{Whether to drop unused levels, as in \code{\link[base:split]{base::split()}}.}

\item{...}{All other arguments of \code{data.table}'s \code{split()} method, i.e.
\code{by}, \code{sorted}, \code{keep.by} and \code{flatten}.}
}
\value{
The list of data tables that \code{data.table} returns.
}
\description{
Reports how many tables the rows were split into, and how many rows each of
them holds.
}
\examples{
dt <- data.table::data.table(g = c("a", "a", "b"), v = 1:3)
split(dt, by = "g")
}
