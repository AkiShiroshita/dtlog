% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{foverlaps}
\alias{foverlaps}
\title{Join two data tables on overlapping intervals, with a log}
\usage{
foverlaps(x, y, ...)
}
\arguments{
\item{x, y}{The data tables to join. Both carry the interval as two columns,
and \code{y} has to be keyed on them.}

\item{...}{All other arguments of \code{\link[data.table:foverlaps]{data.table::foverlaps()}}.}
}
\value{
Whatever \code{\link[data.table:foverlaps]{data.table::foverlaps()}} returns: the matched rows, or,
with \code{which = TRUE}, the row numbers that matched.
}
\description{
Reports how many rows went in and came out, and which columns the join
added. \code{foverlaps()} matches a row of \code{x} against every row of \code{y} whose
interval overlaps it, so the result can be longer as well as shorter than
\code{x}, and the row count is the part worth seeing.
}
\examples{
x <- data.table::data.table(s = c(1, 5), e = c(3, 7))
y <- data.table::data.table(s = c(2, 6), e = c(4, 8))
data.table::setkey(y, s, e)
data.table::foverlaps(x, y)
}
